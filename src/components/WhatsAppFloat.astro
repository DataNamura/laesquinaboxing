---
---

<div class="whatsapp-float">
    <span class="whatsapp-label">Primera clase gratis</span>
    <a 
        href="https://wa.me/34600000000?text=Hola%2C%20me%20gustar%C3%ADa%20información%20sobre%20las%20clases%20de%20boxeo" 
        target="_blank" 
        rel="noopener noreferrer"
        class="whatsapp-btn"
        aria-label="Contactar por WhatsApp"
    >
        <i class="fab fa-whatsapp"></i>
        <span class="whatsapp-text">¡Primera clase gratis!</span>
    </a>
</div>

<style>
    .whatsapp-float {
        position: fixed;
        bottom: 2rem;
        right: 2rem;
        z-index: 1000;
        display: flex;
        flex-direction: column;
        align-items: center;
        gap: 0.5rem;
    }

    .whatsapp-label {
        display: none;
        background-color: rgba(0, 0, 0, 0.8);
        color: white;
        padding: 0.5rem 1rem;
        border-radius: 20px;
        font-size: 0.85rem;
        font-weight: 500;
        letter-spacing: 0.5px;
        box-shadow: 0 2px 5px rgba(0, 0, 0, 0.2);
    }

    @media screen and (max-width: 768px) {
        .whatsapp-label {
            display: block;
        }
    }

    .whatsapp-btn {
        display: flex;
        align-items: center;
        gap: 0.75rem;
        background: linear-gradient(135deg, #25D366, #128C7E);
        color: white;
        padding: 1rem 1.5rem;
        border-radius: 50px;
        text-decoration: none;
        box-shadow: 0 8px 32px rgba(37, 211, 102, 0.3);
        transition: all 0.3s ease;
        font-family: 'Inter', sans-serif;
        font-weight: 600;
        font-size: 0.9rem;
        animation: pulse 2s infinite;
    }

    .whatsapp-btn:hover {
        transform: translateY(-2px);
        box-shadow: 0 12px 40px rgba(37, 211, 102, 0.4);
        background: linear-gradient(135deg, #128C7E, #25D366);
    }

    .whatsapp-btn .fab {
        font-size: 2rem;
    }

    .whatsapp-text {
        white-space: nowrap;
    }

    @keyframes pulse {
        0% {
            box-shadow: 0 8px 32px rgba(37, 211, 102, 0.3);
        }
        50% {
            box-shadow: 0 8px 32px rgba(37, 211, 102, 0.6);
        }
        100% {
            box-shadow: 0 8px 32px rgba(37, 211, 102, 0.3);
        }
    }

    @media screen and (max-width: 768px) {
        .whatsapp-float {
            bottom: 1rem;
            right: 1rem;
        }

        .whatsapp-btn {
            padding: 0.75rem;
            border-radius: 50%;
            width: 60px;
            height: 60px;
            justify-content: center;
        }

        .whatsapp-text {
            display: none;
        }
    }
</style>
